services:
  frontend:
    container_name: arena-frontend-dev
    build:
      context: .
      dockerfile: Dockerfile.dev # Geliştirme için olan Dockerfile'ı kullanır
    ports:
      - "${PORT:-5173}:5173" # .env'de yoksa varsayılan 5173
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:5000}
      - VITE_BASE_DOMAIN=${VITE_BASE_DOMAIN:-localhost:5173}
    volumes:
      - .:/app # Kodunu içeri yansıtır (Hot Reload için şart)
      - /app/node_modules # Konteynerin kendi modüllerini korur (ÖNEMLİ)
